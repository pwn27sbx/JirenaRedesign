<div
  class="select-none slider bg-white/5 backdrop-blur-lg"
  style="
    --width: 230px;
    --height: 130px;
    --quantity: 13;
    --gap: 40px;
    "
>
  <div class="list">
    <img
      class="item"
      style="--position: 1"
      src="./joyglobal.webp"
      alt="Astro"
    />
    <img class="item" style="--position: 2" src="./esco.png" alt="Git" />
    <img class="item" style="--position: 3" src="./sandvik.svg" alt="React" />
    <img
      class="item"
      style="--position: 4"
      src="./stracongym.svg"
      alt="TypeScript"
    />
    <img class="item" style="--position: 5" src="./supermix.svg" alt="NodeJS" />
    <img class="item" style="--position: 6" src="./Titan.png" alt="Tailwind" />
    <img
      class="item"
      style="--position: 7"
      src="./molycop.webp"
      alt="MongoDB"
    />
    <img class="item" style="--position: 8" src="./unimaq.png" alt="CPlus" />
    <img class="item" style="--position: 9" src="./abb.svg" alt="Kotlin" />
    <img class="item" style="--position: 10" src="./famai.png" alt="AWS" />
    <img
      class="item"
      style="--position: 11"
      src="./ferreyros.png"
      alt="Docker"
    />
    <img class="item" style="--position: 12" src="./icc.svg" alt="Redux" />
    <img
      class="item"
      style="--position: 13"
      src="./isopetrol.png"
      alt="JavaScript"
    />
  </div>
</div>

<style>
  .slider {
    width: 100%;
    height: var(--height);
    overflow: hidden;
    mask-image: linear-gradient(
      to right,
      transparent,
      #000 10% 90%,
      transparent
    );
  }
  .slider .list {
    display: flex;
    width: calc((var(--width) + var(--gap)) * var(--quantity));
    min-width: calc((var(--width) + var(--gap)) * var(--quantity));
    position: relative;
    left: 0;
    top: 0;
  }
  .slider .list .item {
    width: var(--width);
    height: var(--height);
    position: absolute;
    left: 100%;
    animation: autoRun 15s linear infinite;
    transition: filter 0.5s;
    animation-delay: calc(
      (15s / var(--quantity)) * (var(--position) - 10)
    ) !important;
    padding: 10px;
  }
  .slider .list .item img {
    width: 100%;
    height: 100%;
    object-fit: contain;
  }
  @keyframes autoRun {
    from {
      left: calc(100% + var(--gap));
    }
    to {
      left: calc((var(--width) + var(--gap)) * -1);
    }
  }

  .slider[reverse="true"] .item {
    animation: reversePlay 15s linear infinite;
  }
  @keyframes reversePlay {
    from {
      left: calc((var(--width) + var(--gap)) * -1);
    }
    to {
      left: calc(100% + var(--gap));
    }
  }
</style>
